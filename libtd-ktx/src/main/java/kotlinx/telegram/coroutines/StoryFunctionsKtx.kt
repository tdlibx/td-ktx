//
// NOTE: THIS FILE IS AUTO-GENERATED by the "TdApiKtxGenerator".kt
// See: https://github.com/tdlibx/td-ktx-generator/
//
package kotlinx.telegram.coroutines

import kotlin.Boolean
import kotlin.ByteArray
import kotlin.Double
import kotlin.Int
import kotlin.IntArray
import kotlin.Long
import kotlin.String
import kotlinx.telegram.core.TelegramFlow
import org.drinkless.tdlib.TdApi
import org.drinkless.tdlib.TdApi.AvailableReactions
import org.drinkless.tdlib.TdApi.CanPostStoryResult
import org.drinkless.tdlib.TdApi.Chats
import org.drinkless.tdlib.TdApi.FormattedText
import org.drinkless.tdlib.TdApi.InputStoryAreas
import org.drinkless.tdlib.TdApi.InputStoryContent
import org.drinkless.tdlib.TdApi.PublicForwards
import org.drinkless.tdlib.TdApi.ReactionType
import org.drinkless.tdlib.TdApi.ReportStoryResult
import org.drinkless.tdlib.TdApi.Stories
import org.drinkless.tdlib.TdApi.Story
import org.drinkless.tdlib.TdApi.StoryAlbum
import org.drinkless.tdlib.TdApi.StoryFullId
import org.drinkless.tdlib.TdApi.StoryInteractions
import org.drinkless.tdlib.TdApi.StoryPrivacySettings
import org.drinkless.tdlib.TdApi.StoryStatistics

/**
 * Suspend function, which activates stealth mode for stories, which hides all views of stories from
 * the current user in the last &quot;story_stealth_mode_past_period&quot; seconds and for the next
 * &quot;story_stealth_mode_future_period&quot; seconds; for Telegram Premium users only.
 */
suspend fun TelegramFlow.activateStoryStealthMode() =
    this.sendFunctionLaunch(TdApi.ActivateStoryStealthMode())

/**
 * Suspend function, which adds stories to the beginning of a previously created story album. If the
 * album is owned by a supergroup or a channel chat, then requires canEditStories administrator right
 * in the chat. Returns the changed album.
 *
 * @param chatId Identifier of the chat that owns the stories.  
 * @param storyAlbumId Identifier of the story album.  
 * @param storyIds Identifier of the stories to add to the album;
 * 1-getOption(&quot;story_album_story_count_max&quot;) identifiers. If after addition the album has
 * more than getOption(&quot;story_album_story_count_max&quot;) stories, then the last one are removed
 * from the album.
 *
 * @return [StoryAlbum] Describes album of stories.
 */
suspend fun TelegramFlow.addStoryAlbumStories(
  chatId: Long,
  storyAlbumId: Int,
  storyIds: IntArray?
): StoryAlbum = this.sendFunctionAsync(TdApi.AddStoryAlbumStories(chatId, storyAlbumId, storyIds))

/**
 * Suspend function, which checks whether the current user can post a story on behalf of a chat;
 * requires canPostStories administrator right for supergroup and channel chats.
 *
 * @param chatId Chat identifier. Pass Saved Messages chat identifier when posting a story on behalf
 * of the current user.
 *
 * @return [CanPostStoryResult] This class is an abstract base class.
 */
suspend fun TelegramFlow.canPostStory(chatId: Long): CanPostStoryResult =
    this.sendFunctionAsync(TdApi.CanPostStory(chatId))

/**
 * Suspend function, which informs TDLib that a story is closed by the user.
 *
 * @param storyPosterChatId The identifier of the poster of the story to close.  
 * @param storyId The identifier of the story.
 */
suspend fun TelegramFlow.closeStory(storyPosterChatId: Long, storyId: Int) =
    this.sendFunctionLaunch(TdApi.CloseStory(storyPosterChatId, storyId))

/**
 * Suspend function, which creates an album of stories; requires canEditStories administrator right
 * for supergroup and channel chats.
 *
 * @param storyPosterChatId Identifier of the chat that posted the stories.  
 * @param name Name of the album; 1-12 characters.  
 * @param storyIds Identifiers of stories to add to the album;
 * 0-getOption(&quot;story_album_story_count_max&quot;) identifiers.
 *
 * @return [StoryAlbum] Describes album of stories.
 */
suspend fun TelegramFlow.createStoryAlbum(
  storyPosterChatId: Long,
  name: String?,
  storyIds: IntArray?
): StoryAlbum = this.sendFunctionAsync(TdApi.CreateStoryAlbum(storyPosterChatId, name, storyIds))

/**
 * Suspend function, which deletes a story posted by the bot on behalf of a business account; for
 * bots only.
 *
 * @param businessConnectionId Unique identifier of business connection.  
 * @param storyId Identifier of the story to delete.
 */
suspend fun TelegramFlow.deleteBusinessStory(businessConnectionId: String?, storyId: Int) =
    this.sendFunctionLaunch(TdApi.DeleteBusinessStory(businessConnectionId, storyId))

/**
 * Suspend function, which deletes a previously posted story. Can be called only if
 * story.canBeDeleted == true.
 *
 * @param storyPosterChatId Identifier of the chat that posted the story.  
 * @param storyId Identifier of the story to delete.
 */
suspend fun TelegramFlow.deleteStory(storyPosterChatId: Long, storyId: Int) =
    this.sendFunctionLaunch(TdApi.DeleteStory(storyPosterChatId, storyId))

/**
 * Suspend function, which deletes a story album. If the album is owned by a supergroup or a channel
 * chat, then requires canEditStories administrator right in the chat.
 *
 * @param chatId Identifier of the chat that owns the stories.  
 * @param storyAlbumId Identifier of the story album.
 */
suspend fun TelegramFlow.deleteStoryAlbum(chatId: Long, storyAlbumId: Int) =
    this.sendFunctionLaunch(TdApi.DeleteStoryAlbum(chatId, storyAlbumId))

/**
 * Suspend function, which changes a story posted by the bot on behalf of a business account; for
 * bots only.
 *
 * @param storyPosterChatId Identifier of the chat that posted the story.  
 * @param storyId Identifier of the story to edit.  
 * @param content New content of the story.  
 * @param areas New clickable rectangle areas to be shown on the story media.  
 * @param caption New story caption.  
 * @param privacySettings The new privacy settings for the story.
 *
 * @return [Story] Represents a story.
 */
suspend fun TelegramFlow.editBusinessStory(
  storyPosterChatId: Long,
  storyId: Int,
  content: InputStoryContent?,
  areas: InputStoryAreas?,
  caption: FormattedText?,
  privacySettings: StoryPrivacySettings?
): Story = this.sendFunctionAsync(TdApi.EditBusinessStory(storyPosterChatId, storyId, content,
    areas, caption, privacySettings))

/**
 * Suspend function, which changes content and caption of a story. Can be called only if
 * story.canBeEdited == true.
 *
 * @param storyPosterChatId Identifier of the chat that posted the story.  
 * @param storyId Identifier of the story to edit.  
 * @param content New content of the story; pass null to keep the current content.  
 * @param areas New clickable rectangle areas to be shown on the story media; pass null to keep the
 * current areas. Areas can't be edited if story content isn't changed.  
 * @param caption New story caption; pass null to keep the current caption.
 */
suspend fun TelegramFlow.editStory(
  storyPosterChatId: Long,
  storyId: Int,
  content: InputStoryContent?,
  areas: InputStoryAreas?,
  caption: FormattedText?
) = this.sendFunctionLaunch(TdApi.EditStory(storyPosterChatId, storyId, content, areas, caption))

/**
 * Suspend function, which changes cover of a video story. Can be called only if story.canBeEdited
 * == true and the story isn't being edited now.
 *
 * @param storyPosterChatId Identifier of the chat that posted the story.  
 * @param storyId Identifier of the story to edit.  
 * @param coverFrameTimestamp New timestamp of the frame, which will be used as video thumbnail.
 */
suspend fun TelegramFlow.editStoryCover(
  storyPosterChatId: Long,
  storyId: Int,
  coverFrameTimestamp: Double
) = this.sendFunctionLaunch(TdApi.EditStoryCover(storyPosterChatId, storyId, coverFrameTimestamp))

/**
 * Suspend function, which returns a story.
 *
 * @param storyPosterChatId Identifier of the chat that posted the story.  
 * @param storyId Story identifier.  
 * @param onlyLocal Pass true to get only locally available information without sending network
 * requests.
 *
 * @return [Story] Represents a story.
 */
suspend fun TelegramFlow.getStory(
  storyPosterChatId: Long,
  storyId: Int,
  onlyLocal: Boolean
): Story = this.sendFunctionAsync(TdApi.GetStory(storyPosterChatId, storyId, onlyLocal))

/**
 * Suspend function, which returns the list of stories added to the given story album. For optimal
 * performance, the number of returned stories is chosen by TDLib.
 *
 * @param chatId Chat identifier.  
 * @param storyAlbumId Story album identifier.  
 * @param offset Offset of the first entry to return; use 0 to get results from the first album
 * story.  
 * @param limit The maximum number of stories to be returned. For optimal performance, the number of
 * returned stories is chosen by TDLib and can be smaller than the specified limit.
 *
 * @return [Stories] Represents a list of stories.
 */
suspend fun TelegramFlow.getStoryAlbumStories(
  chatId: Long,
  storyAlbumId: Int,
  offset: Int,
  limit: Int
): Stories = this.sendFunctionAsync(TdApi.GetStoryAlbumStories(chatId, storyAlbumId, offset, limit))

/**
 * Suspend function, which returns reactions, which can be chosen for a story.
 *
 * @param rowSize Number of reaction per row, 5-25.
 *
 * @return [AvailableReactions] Represents a list of reactions that can be added to a message.
 */
suspend fun TelegramFlow.getStoryAvailableReactions(rowSize: Int): AvailableReactions =
    this.sendFunctionAsync(TdApi.GetStoryAvailableReactions(rowSize))

/**
 * Suspend function, which returns interactions with a story. The method can be called only for
 * stories posted on behalf of the current user.
 *
 * @param storyId Story identifier.  
 * @param query Query to search for in names, usernames and titles; may be empty to get all relevant
 * interactions.  
 * @param onlyContacts Pass true to get only interactions by contacts; pass false to get all
 * relevant interactions.  
 * @param preferForwards Pass true to get forwards and reposts first, then reactions, then other
 * views; pass false to get interactions sorted just by interaction date.  
 * @param preferWithReaction Pass true to get interactions with reaction first; pass false to get
 * interactions sorted just by interaction date. Ignored if preferForwards == true.  
 * @param offset Offset of the first entry to return as received from the previous request; use
 * empty string to get the first chunk of results.  
 * @param limit The maximum number of story interactions to return.
 *
 * @return [StoryInteractions] Represents a list of interactions with a story.
 */
suspend fun TelegramFlow.getStoryInteractions(
  storyId: Int,
  query: String?,
  onlyContacts: Boolean,
  preferForwards: Boolean,
  preferWithReaction: Boolean,
  offset: String?,
  limit: Int
): StoryInteractions = this.sendFunctionAsync(TdApi.GetStoryInteractions(storyId, query,
    onlyContacts, preferForwards, preferWithReaction, offset, limit))

/**
 * Suspend function, which returns the list of chats with non-default notification settings for
 * stories.
 *
 * @return [Chats] Represents a list of chats.
 */
suspend fun TelegramFlow.getStoryNotificationSettingsExceptions(): Chats =
    this.sendFunctionAsync(TdApi.GetStoryNotificationSettingsExceptions())

/**
 * Suspend function, which returns forwards of a story as a message to public chats and reposts by
 * public channels. Can be used only if the story is posted on behalf of the current user or
 * story.canGetStatistics == true. For optimal performance, the number of returned messages and stories
 * is chosen by TDLib.
 *
 * @param storyPosterChatId The identifier of the poster of the story.  
 * @param storyId The identifier of the story.  
 * @param offset Offset of the first entry to return as received from the previous request; use
 * empty string to get the first chunk of results.  
 * @param limit The maximum number of messages and stories to be returned; must be positive and
 * can't be greater than 100. For optimal performance, the number of returned objects is chosen by
 * TDLib and can be smaller than the specified limit.
 *
 * @return [PublicForwards] Represents a list of public forwards and reposts as a story of a message
 * or a story.
 */
suspend fun TelegramFlow.getStoryPublicForwards(
  storyPosterChatId: Long,
  storyId: Int,
  offset: String?,
  limit: Int
): PublicForwards = this.sendFunctionAsync(TdApi.GetStoryPublicForwards(storyPosterChatId, storyId,
    offset, limit))

/**
 * Suspend function, which returns detailed statistics about a story. Can be used only if
 * story.canGetStatistics == true.
 *
 * @param chatId Chat identifier.  
 * @param storyId Story identifier.  
 * @param isDark Pass true if a dark theme is used by the application.
 *
 * @return [StoryStatistics] A detailed statistics about a story.
 */
suspend fun TelegramFlow.getStoryStatistics(
  chatId: Long,
  storyId: Int,
  isDark: Boolean
): StoryStatistics = this.sendFunctionAsync(TdApi.GetStoryStatistics(chatId, storyId, isDark))

/**
 * Suspend function, which informs TDLib that a story is opened and is being viewed by the user.
 *
 * @param storyPosterChatId The identifier of the chat that posted the opened story.  
 * @param storyId The identifier of the story.
 */
suspend fun TelegramFlow.openStory(storyPosterChatId: Long, storyId: Int) =
    this.sendFunctionLaunch(TdApi.OpenStory(storyPosterChatId, storyId))

/**
 * Suspend function, which posts a new story on behalf of a chat; requires canPostStories
 * administrator right for supergroup and channel chats. Returns a temporary story.
 *
 * @param chatId Identifier of the chat that will post the story. Pass Saved Messages chat
 * identifier when posting a story on behalf of the current user.  
 * @param content Content of the story.  
 * @param areas Clickable rectangle areas to be shown on the story media; pass null if none.  
 * @param caption Story caption; pass null to use an empty caption;
 * 0-getOption(&quot;story_caption_length_max&quot;) characters; can have entities only if
 * getOption(&quot;can_use_text_entities_in_story_caption&quot;).  
 * @param privacySettings The privacy settings for the story; ignored for stories posted on behalf
 * of supergroup and channel chats.  
 * @param albumIds Identifiers of story albums to which the story will be added upon posting. An
 * album can have up to getOption(&quot;story_album_story_count_max&quot;).  
 * @param activePeriod Period after which the story is moved to archive, in seconds; must be one of
 * 6 * 3600, 12 * 3600, 86400, or 2 * 86400 for Telegram Premium users, and 86400 otherwise.  
 * @param fromStoryFullId Full identifier of the original story, which content was used to create
 * the story; pass null if the story isn't repost of another story.  
 * @param isPostedToChatPage Pass true to keep the story accessible after expiration.  
 * @param protectContent Pass true if the content of the story must be protected from forwarding and
 * screenshotting.
 *
 * @return [Story] Represents a story.
 */
suspend fun TelegramFlow.postStory(
  chatId: Long,
  content: InputStoryContent?,
  areas: InputStoryAreas?,
  caption: FormattedText?,
  privacySettings: StoryPrivacySettings?,
  albumIds: IntArray?,
  activePeriod: Int,
  fromStoryFullId: StoryFullId?,
  isPostedToChatPage: Boolean,
  protectContent: Boolean
): Story = this.sendFunctionAsync(TdApi.PostStory(chatId, content, areas, caption, privacySettings,
    albumIds, activePeriod, fromStoryFullId, isPostedToChatPage, protectContent))

/**
 * Suspend function, which removes stories from an album. If the album is owned by a supergroup or a
 * channel chat, then requires canEditStories administrator right in the chat. Returns the changed
 * album.
 *
 * @param chatId Identifier of the chat that owns the stories.  
 * @param storyAlbumId Identifier of the story album.  
 * @param storyIds Identifier of the stories to remove from the album.
 *
 * @return [StoryAlbum] Describes album of stories.
 */
suspend fun TelegramFlow.removeStoryAlbumStories(
  chatId: Long,
  storyAlbumId: Int,
  storyIds: IntArray?
): StoryAlbum = this.sendFunctionAsync(TdApi.RemoveStoryAlbumStories(chatId, storyAlbumId,
    storyIds))

/**
 * Suspend function, which changes order of stories in an album. If the album is owned by a
 * supergroup or a channel chat, then requires canEditStories administrator right in the chat. Returns
 * the changed album.
 *
 * @param chatId Identifier of the chat that owns the stories.  
 * @param storyAlbumId Identifier of the story album.  
 * @param storyIds Identifier of the stories to move to the beginning of the album. All other
 * stories are placed in the current order after the specified stories.
 *
 * @return [StoryAlbum] Describes album of stories.
 */
suspend fun TelegramFlow.reorderStoryAlbumStories(
  chatId: Long,
  storyAlbumId: Int,
  storyIds: IntArray?
): StoryAlbum = this.sendFunctionAsync(TdApi.ReorderStoryAlbumStories(chatId, storyAlbumId,
    storyIds))

/**
 * Suspend function, which changes order of story albums. If the albums are owned by a supergroup or
 * a channel chat, then requires canEditStories administrator right in the chat.
 *
 * @param chatId Identifier of the chat that owns the stories.  
 * @param storyAlbumIds New order of story albums.
 */
suspend fun TelegramFlow.reorderStoryAlbums(chatId: Long, storyAlbumIds: IntArray?) =
    this.sendFunctionLaunch(TdApi.ReorderStoryAlbums(chatId, storyAlbumIds))

/**
 * Suspend function, which reports a story to the Telegram moderators.
 *
 * @param storyPosterChatId The identifier of the poster of the story to report.  
 * @param storyId The identifier of the story to report.  
 * @param optionId Option identifier chosen by the user; leave empty for the initial request.  
 * @param text Additional report details; 0-1024 characters; leave empty for the initial request.
 *
 * @return [ReportStoryResult] This class is an abstract base class.
 */
suspend fun TelegramFlow.reportStory(
  storyPosterChatId: Long,
  storyId: Int,
  optionId: ByteArray?,
  text: String?
): ReportStoryResult = this.sendFunctionAsync(TdApi.ReportStory(storyPosterChatId, storyId,
    optionId, text))

/**
 * Suspend function, which changes name of an album of stories. If the album is owned by a
 * supergroup or a channel chat, then requires canEditStories administrator right in the chat. Returns
 * the changed album.
 *
 * @param chatId Identifier of the chat that owns the stories.  
 * @param storyAlbumId Identifier of the story album.  
 * @param name New name of the album; 1-12 characters.
 *
 * @return [StoryAlbum] Describes album of stories.
 */
suspend fun TelegramFlow.setStoryAlbumName(
  chatId: Long,
  storyAlbumId: Int,
  name: String?
): StoryAlbum = this.sendFunctionAsync(TdApi.SetStoryAlbumName(chatId, storyAlbumId, name))

/**
 * Suspend function, which changes privacy settings of a story. The method can be called only for
 * stories posted on behalf of the current user and if story.canBeEdited == true.
 *
 * @param storyId Identifier of the story.  
 * @param privacySettings The new privacy settings for the story.
 */
suspend fun TelegramFlow.setStoryPrivacySettings(storyId: Int,
    privacySettings: StoryPrivacySettings?) =
    this.sendFunctionLaunch(TdApi.SetStoryPrivacySettings(storyId, privacySettings))

/**
 * Suspend function, which changes chosen reaction on a story that has already been sent.
 *
 * @param storyPosterChatId The identifier of the poster of the story.  
 * @param storyId The identifier of the story.  
 * @param reactionType Type of the reaction to set; pass null to remove the reaction. Custom emoji
 * reactions can be used only by Telegram Premium users. Paid reactions can't be set.  
 * @param updateRecentReactions Pass true if the reaction needs to be added to recent reactions.
 */
suspend fun TelegramFlow.setStoryReaction(
  storyPosterChatId: Long,
  storyId: Int,
  reactionType: ReactionType?,
  updateRecentReactions: Boolean
) = this.sendFunctionLaunch(TdApi.SetStoryReaction(storyPosterChatId, storyId, reactionType,
    updateRecentReactions))
